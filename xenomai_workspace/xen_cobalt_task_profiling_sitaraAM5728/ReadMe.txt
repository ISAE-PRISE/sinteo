How to run:

1. Give execution permissions (if needed) to make_n_run.sh --> chmod +x make_n_run.sh
2. Execute shell script --> ./make_n_run.sh


Description:

make_n_run.sh: Cleans, compiles, prepares and run
